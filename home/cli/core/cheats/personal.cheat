; Catch-all for one-off snippets tied to tools you rarely touch.
; Domain snippets live in their own file, one per domain (vcs, containers, ...).

% personal

# Pretty-print JSON from the clipboard (macOS)
pbpaste | jq .
