# {{ project_name }}

{{ description }}

## Develop

The only prerequisite is [mise](https://mise.jdx.dev) — it provides every tool.

```sh
mise install      # Go toolchain, lefthook, committed
lefthook install  # wire git hooks
mise run test
mise run build
```
