pre-commit:
  commands:
    fmt:
      glob: "*.go"
      run: gofmt -w {staged_files}
      stage_fixed: true
    lint:
      run: mise run lint

# conventional-commit linting. committed auto-discovers ./committed.toml
commit-msg:
  commands:
    committed:
      run: committed --commit-file {1}
