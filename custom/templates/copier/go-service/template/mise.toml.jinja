# mise is the ONLY prerequisite here, it brings every tool. so contributors
# dont need nix/homebrew, `mise install` sets them all up.
[tools]
go = "{{ go_version }}"
lefthook = "latest"
"ubi:crate-ci/committed" = "latest" # commit-msg hook linter

[tasks.test]
description = "Run tests"
run = "go test ./..."

[tasks.lint]
description = "Vet the code"
run = "go vet ./..."

[tasks.build]
description = "Build the binary into ./bin"
run = "go build -o bin/{{ project_name }} ."

[tasks.run]
description = "Run the service"
run = "go run ."
